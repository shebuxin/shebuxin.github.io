source "https://rubygems.org"

# Match the dependency set used by GitHub Pages. This gem already includes the
# supported Jekyll plugins configured in _config.yml.
gem "github-pages", "~> 232", group: :jekyll_plugins
